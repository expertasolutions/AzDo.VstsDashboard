apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vstsDashboard.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "vstsDashboard.name" . }}
    helm.sh/chart: {{ include "vstsDashboard.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "vstsDashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never